name: database_repository
description: A database independent repository style implementation for dart and flutter
version: 1.2.2
repository: https://github.com/glassp/database_repository

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  meta: ^1.7.0
dev_dependencies:
  effective_dart: ^1.3.2
  test: ^1.20.1
